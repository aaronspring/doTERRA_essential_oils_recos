# AGENTS

## General
- always use environment `.venv` generated by `uv`. To execute code python code, use `uv run python <script>`
- never `pip install` instead use `uv add <package>`
- never `git add .`
- use playwright MCP to access the web ui